# gehört zu Context_Handler
# Das Context_Handler directory wurde vollständig von Marvin Palsbröker erstellt

# Mit KI erstellt und abgestimmt (Github Copilot)
# Für Docker bewusst gepinnt, damit keine unerwarteten CUDA/NVIDIA-Abhängigkeiten gezogen werden.

--extra-index-url https://download.pytorch.org/whl/cpu

# Flask Version für den Webserver
Flask==3.1.3

# LlamaIndex - nur noch für Embeddings / Settings genutzt
llama-index-core==0.10.45
llama-index-embeddings-huggingface==0.2.3

# Vector Database
qdrant-client==1.17.0

# Embeddings (CPU-only)
torch==2.3.1+cpu
sentence-transformers==2.7.0
transformers==4.41.2
huggingface-hub==0.23.4

# PDF Processing
pypdf==4.3.1

# Utilities
python-dotenv==1.0.0
tqdm==4.67.1

# Core
numpy==1.26.4
pydantic==2.8.2