# Root-level docker-compose that starts both microservices on a shared network
# so they can reach each other by service name.
#
# Usage:
#   cp .env.example .env   # fill in all secrets once
#   docker compose up --build
#
# Context_Handler is built from its own directory/Dockerfile (not modified).
# Request_Handler is built from its own directory/Dockerfile (not modified).
# CONTEXT_HANDLER_URL is injected so the request-handler can reach the
# context-handler container via Docker's internal DNS.

services:

  # ── Context Handler ──────────────────────────────────────────────────────────
  context-handler:
    build:
      context: ./Context_Handler
      dockerfile: Dockerfile
    container_name: context-handler
    ports:
      - "5000:5000"
    volumes:
      - ./Context_Handler/pdfs:/app/pdfs
      # hf_cache volume absichtlich entfernt:
      # Das Embedding-Modell wird bereits beim Image-Build nach /opt/hf-cache
      # vorinstalliert (siehe Dockerfile Build-Stage). Ein Named-Volume an
      # derselben Stelle würde beim ersten Start das leere Volume über den
      # vorgebackenen Cache legen und das Modell wäre weg.
    environment:
      - FLASK_APP=context_webserver.py
      - FLASK_ENV=production
      - HF_HOME=/opt/hf-cache
      - TRANSFORMERS_CACHE=/opt/hf-cache
      - SENTENCE_TRANSFORMERS_HOME=/opt/hf-cache
      - QDRANT_URL=${QDRANT_URL}
      - QDRANT_API_KEY=${QDRANT_API_KEY}
      - HF_TOKEN=${HF_TOKEN}
      - WEBSERVER_TOKEN=${WEBSERVER_TOKEN}
    restart: unless-stopped
    networks:
      - rag-network
    deploy:
      resources:
        limits:
          memory: 4G
        reservations:
          memory: 2G
          # GPU-Zugriff via NVIDIA Container Runtime (nvidia-container-toolkit
          # muss auf dem Host installiert sein).
          # Falls keine GPU vorhanden: diesen Block auskommentieren.
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]



  # ── Request Handler ──────────────────────────────────────────────────────────
  request-handler:
    build:
      context: ./Request_Handler
      dockerfile: Dockerfile
    container_name: request-handler-cont
    ports:
      - "8000:8000"
    environment:
      - AZURE_OPENAI_ENDPOINT=${AZURE_OPENAI_ENDPOINT}
      - AZURE_OPENAI_API_KEY=${AZURE_OPENAI_API_KEY}
      - AZURE_OPENAI_DEPLOYMENT=${AZURE_OPENAI_DEPLOYMENT}
      # Override the URL so this container reaches context-handler by service name
      - CONTEXT_HANDLER_URL=${CONTEXT_HANDLER_URL}
      # Reuse the same shared token that context-handler expects
      - CONTEXT_HANDLER_TOKEN=${CONTEXT_HANDLER_TOKEN}
    depends_on:
      - context-handler
    restart: unless-stopped
    networks:
      - rag-network



# ── Webpage ───────────────────────────────────────────────────────────────
  webpage:
    build:
      context: ./webpage
      dockerfile: Dockerfile
    container_name: webpage_deployment
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - rag-network



# ── Shared Network ─────────────────────────────────────────────────────────
networks:
  rag-network:
    driver: bridge

# ── Shared Volumes ─────────────────────────────────────────────────────────
# hf_cache wurde entfernt – das Modell liegt direkt im Image-Layer.
  